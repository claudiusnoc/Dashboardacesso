-- Esta versão foi aplicada uma única vez no projeto hospedado para migrar
-- registros do dashboard legado. O payload operacional foi intencionalmente
-- omitido do repositório público; os dados permanecem no PostgreSQL.

do $$
begin
  null;
end;
$$;
